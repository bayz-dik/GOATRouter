{
  "name": "bayz-router-handoff",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "runtime:test": "npm run test --workspaces --if-present",
    "runtime:build": "npm run build --workspace @bayz/contracts --if-present && npm run build --workspace @bayz/security --if-present && npm run build --workspace @bayz/storage --if-present && npm run build --workspace @bayz/providers --if-present && npm run build --workspace @bayz/proxy --if-present && npm run build --workspace @bayz/router --if-present && npm run build --workspace @bayz/dashboard && npm run build --workspace @bayz/server",
    "runtime:verify": "npm run runtime:test && npm run runtime:build"
  },
  "devDependencies": {
    "@types/node": "^26.3.0",
    "tsx": "^4.23.12",
    "typescript": "^5.9.3"
  }
}
